import {useState} from 'react';
import {Flex} from '@chakra-ui/react';

type Props = {};

export function AsistenteIngresoTerminados(props: Props) {
    return (
        <Flex>

        </Flex>
    );
}
